################################################################################
# Use this file to set the default hydra configuration

defaults:
  - override job_logging: colorlog
  - override hydra_logging: colorlog

run:
  dir: runs/${now:%Y-%m-%d}/${now:%H-%M-%S}
